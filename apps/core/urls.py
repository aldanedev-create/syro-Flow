from django.urls import path
from . import views

app_name = 'core'

urlpatterns = [
    # Core URLs (home is handled at project level)
    # path('', views.home, name='home'),
    # Add any core-specific URLs here
]